{
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  },
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "crons": [
    {
      "path": "/api/cron/ripasso-notify",
      "schedule": "* * * * *"
    }
  ]
}
